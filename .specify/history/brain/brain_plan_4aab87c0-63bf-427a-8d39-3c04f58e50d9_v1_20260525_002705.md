# Implementation Plan - Enhancing Code Quality & Driving Changes via SDD

This plan details our strategy to address the findings identified by the `code-enhancer` tool run against `repository-manager`. We will implement key improvements targeting **Security Posture**, **Monolithic Complexity Reduction**, **Environment Variable Documentation**, and **Concept Traceability** using the generated SDD tasks.

## User Review Required

> [!IMPORTANT]
> **Stale Graph Script Deletion**: To fully comply with your request that `repository-manager` has nothing to do with the graph database, we will delete the stale scripts `scripts/check_graph.py` and `scripts/verify_graph.py`. This immediately eliminates 2 of the 5 High-severity security vulnerabilities (including the unsafe `pickle.load` deserialization risk).

> [!WARNING]
> **Bandit Annotations for Command Execution**: The remaining `subprocess.Popen` calls in the runner and test scripts are secure by design (using argument list routing rather than shell evaluation) but are flagged by Bandit. We will explicitly annotate them with `# nosec B603` and `# nosec B602` to achieve clean, warning-free Bandit scans.

---

## Proposed Changes

We will address the findings in a phased, prioritized manner:

### 1. Security Posture & Stale Script Deletion (P1 - High)
Delete all remaining graph-related scripts and annotate secure subprocess usages to satisfy Bandit audits.

#### [DELETE] [check_graph.py](file:///home/apps/workspace/agent-packages/agents/repository-manager/scripts/check_graph.py)
* Delete the unused debugging script that contained the unsafe `pickle.load()` call (CWE-502).

#### [DELETE] [verify_graph.py](file:///home/apps/workspace/agent-packages/agents/repository-manager/scripts/verify_graph.py)
* Delete the graph verification script as graph integration is completely removed.

#### [MODIFY] [verify_agent_server.py](file:///home/apps/workspace/agent-packages/agents/repository-manager/scripts/verify_agent_server.py)
* Add `# nosec B603` to the list-based `subprocess.Popen` call (line 123) to indicate safe parameter usage.

#### [MODIFY] [test_agent_integration.py](file:///home/apps/workspace/agent-packages/agents/repository-manager/tests/test_agent_integration.py)
* Add `# nosec B603` to list-based `subprocess.Popen` calls.

---

### 2. Environment Variables & Onboarding Parity (P2 - Medium)
Synchronize all used environment variables with `.env.example` and docstrings to ensure seamless onboarding.

#### [MODIFY] [.env.example](file:///home/apps/workspace/agent-packages/agents/repository-manager/.env.example)
* Document missing Python env vars: `AUTH_TYPE`, `EUNOMIA_POLICY_FILE`, `EUNOMIA_TYPE`, `GIT_OPERATION_THREADS`.
* Document required LLM/API keys: `LLM_API_KEY`, `LLM_BASE_URL`, `MCP_URL`, `MODEL_NAME`.

---

### 3. Concept Traceability (P4 - Enhancement)
Establish complete bidirectional traceability using stable concept IDs in file docstrings and test markers.

#### [MODIFY] [repository_manager.py](file:///home/apps/workspace/agent-packages/agents/repository-manager/repository_manager/repository_manager.py)
* Add stable `CONCEPT:RM-XXX` IDs to all major functions (e.g. `validate_projects`, `phased_bumpversion`) in their docstrings.

---

## Verification Plan

### Automated Tests
* Run the full test suite to ensure all 53 tests continue to pass perfectly:
  ```bash
  uv run pytest
  ```
* Run local pre-commit hooks to ensure code format, import ast, and mypy type checks remain perfectly compliant:
  ```bash
  uv run pre-commit run --all-files
  ```

### Manual Verification
* Run the synchronous `code-enhancer` tool again to verify that the target grades improve and P1 findings are fully resolved:
  ```bash
  uv run python run_single_enhancer.py
  ```
