# Implementation Plan - arr-mcp Code Quality & Compliance Enhancements

We will address the key findings from our sequential Code Enhancer analysis to significantly raise the repository's overall GPA and engineering posture. These improvements will focus on environment variable safety/documentation, testing excellence, changelog governance, internal documentation parity, and concept traceability without changing production features or behavior.

---

## User Review Required

> [!NOTE]
> All changes are strictly additive and documentation/test-focused. There are zero functional modifications or breaking changes to production endpoints or tools.

---

## Proposed Changes

We will execute enhancements across 5 core areas identified by the Code Enhancer scorecard:

### 1. Environment Variable Safety & Completeness
- **[MODIFY] [.env.example](file:///home/apps/workspace/agent-packages/agents/arr-mcp/.env.example)**
  - Populate the template with the 27 Python environment variables scanned from `arr_mcp/auth.py` and the client/agent builders:
    - Service base URLs, API keys, SSL verification flags (`*_BASE_URL`, `*_API_KEY`, `*_SSL_VERIFY`, `*_TOKEN`) for all 7 media services (Sonarr, Radarr, Lidarr, Prowlarr, Bazarr, Seerr, Chaptarr).
    - `ALLOWED_CLIENT_REDIRECT_URIS`, `AUTH_TYPE`, `DEFAULT_AGENT_NAME`.
- **[MODIFY] [README.md](file:///home/apps/workspace/agent-packages/agents/arr-mcp/README.md)**
  - Add a comprehensive "Environment Variables" documentation table mapping each variable to its service component, description, and default security posture.

### 2. Pytest Quality & Shared Fixtures
- **[NEW] [conftest.py](file:///home/apps/workspace/agent-packages/agents/arr-mcp/tests/conftest.py)**
  - Create a standard `conftest.py` in the `tests/` directory.
  - Define reusable pytest fixtures for mocking `requests.Session` responses, standard client constructors, environment states, and Mock Starlette Requests.
  - Refactor test suites to leverage these shared fixtures, minimizing boilerplate and increasing fixture usage metrics (addressing T029/T030/T031).

### 3. Documentation Governance & Navigation
- **[MODIFY] [README.md](file:///home/apps/workspace/agent-packages/agents/arr-mcp/README.md)**
  - Add a clean Table of Contents for seamless navigation.
  - Fix 2 broken internal links:
    - `docs/mcp.md` -> `docs/index.md#mcp`
    - `docs/agent.md` -> `docs/index.md#a2a-agent`
  - Incorporate a modern, detailed "Usage & Quick Start" section detailing both the MCP and Agent servers with copy-pasteable execution examples.

### 4. Keep a Changelog Compliance
- **[MODIFY] [CHANGELOG.md](file:///home/apps/workspace/agent-packages/agents/arr-mcp/CHANGELOG.md)**
  - Format `CHANGELOG.md` to be fully parseable and compliant with the "Keep a Changelog" standard.
  - Add a major changelog entry for version `0.15.0` documenting the recent test coverage achievement (100% North Star) and the current code quality enhancements.

### 5. Concept Traceability Integration
- **[MODIFY] Codebase files (`arr_mcp/auth.py`, `arr_mcp/mcp_server.py`, `arr_mcp/agent_server.py`, `tests/`)**
  - Integrate inline concept markers (like `CONCEPT:ECO-4.0` for MCP tools and `CONCEPT:OS-5.1` / `CONCEPT:OS-5.1` for security/audit elements) into the docstrings of all major functions and their matching test cases to satisfy traceability parity.

---

## Verification Plan

### Automated Verification
1. Validate type-safety, formatting, and lint rules using:
   ```bash
   pre-commit run --all-files
   ```
2. Execute the entire pytest suite to ensure 100% test passing and 100% code coverage:
   ```bash
   /home/apps/workspace/.venv/bin/pytest --cov=arr_mcp --cov-report=term-missing
   ```

### Manual Verification
1. Inspect `README.md` and `CHANGELOG.md` to verify formatting looks professional and all links compile correctly.
