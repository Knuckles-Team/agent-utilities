# Implementation Plan - Resolving Cypher Keyword Issues & Validating X Post Ingestion

This plan outlines the steps required to fix the Cypher keyword parsing exception in `LadybugDB` (Kuzu) and successfully execute and test the `x_posts_to_kg_ingestion` workflow.

## User Review Required

We are addressing a database parser bug where Cypher keywords (such as `order`) are used as property keys without backtick escaping, causing parsing exceptions. We will enable proper backtick escaping for all property keys, which we have verified works correctly in the Kuzu engine.

## Proposed Changes

### Database Engine

#### [MODIFY] [engine.py](file:///home/apps/workspace/agent-packages/agent-utilities/agent_utilities/knowledge_graph/core/engine.py)
- Update `_get_set_clause` and `_upsert_node` to always wrap property keys in backticks (e.g. `` `order` ``) when formatting `SET` clauses and `CREATE` map literals, resolving Kuzu reserved keyword parser exceptions.

## Verification Plan

### Automated Tests
- Run `pytest tests/unit/knowledge_graph` to verify that all unit tests (including `test_ladybug_backend_escaped_properties`) pass successfully.
- Trigger the workflow compilation / ingestion to ensure workflows build without database errors.

### Manual Verification
- Execute the `x_posts_to_kg_ingestion` workflow using the URL `https://x.com/i/status/2057129225593741768`.
- Query the graph to verify that the workflow and steps are persisted and that the browsed post details are correctly ingested.
