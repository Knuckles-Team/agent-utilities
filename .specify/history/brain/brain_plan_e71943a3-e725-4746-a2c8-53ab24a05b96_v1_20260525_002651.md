# Implementation Plan — Workspace Validation Compliance (Fix and Validate Only)

The goal is to achieve 100% compliance across all 43 repositories in the workspace by resolving the pre-commit, codespell, and testing failures in the **8 currently failing projects**.

Consistent with the user's instructions, we will **only fix and validate**, avoiding any phased version bumps or git pushes.

---

## User Review Required

> [!NOTE]
> All changes are non-breaking lint, import, and test-parity fixes that restore standard compliance to split-file MCP architectures and ignore words in codespell. No API schemas or business logic are altered.

---

## Proposed Changes

### 1. `agent-utilities`

Register the missing pillar concepts `ECO-4.12` and `ECO-4.14` in the master documentation so that `skill-graphs` parity checks pass.

#### [MODIFY] [docs/overview.md](file:///home/apps/workspace/agent-packages/agent-utilities/docs/overview.md)
- In the **Pillar 4: Ecosystem & Peripherals** subgraph:
  ```diff
          ECO410["<b>ECO-4.10: Agent Ingestor</b>"]
          ECO411["<b>ECO-4.11: MCP Discovery</b>"]
+         ECO412["<b>ECO-4.12: Self-Documenting Skill-Graph</b>"]
+         ECO414["<b>ECO-4.14: Infrastructure Blueprint Library</b>"]
      end
  ```
- In the Pillar 4 table:
  ```diff
  | ECO-4.10 | Agent Toolkit Ingestor | Ingest and persist external agent toolkits |
  | ECO-4.11 | MCP Live Discovery | Real-time MCP capability discovery and mapping |
+ | ECO-4.12 | Self-Documenting Skill-Graph | Automated crawling and structured skill-graph documentation |
+ | ECO-4.14 | Infrastructure Blueprint Library | Library of modular, declarative system infrastructure configurations |
  ```

---

### 2. `universal-skills`

Ignore false spelling positives for `HSA` (Health Savings Account) and `HASS` (Home Assistant).

#### [MODIFY] [.codespellignore](file:///home/apps/workspace/agent-packages/skills/universal-skills/.codespellignore)
- Append `HSA` and `HASS` to the file:
  ```diff
  nin
  nam
  tread
  ot
+ HSA
+ HASS
  ```

---

### 3. `arr-mcp`

Exclude the auto-generated `.specify/` folder from being scanned by codespell.

#### [MODIFY] [.pre-commit-config.yaml](file:///home/apps/workspace/agent-packages/agents/arr-mcp/.pre-commit-config.yaml)
- Update codespell exclude pattern to ignore `\.specify/`:
  ```diff
  -    exclude: ^(tests/|test/|scripts/|script/|.*lock.*)
+    exclude: ^(tests/|test/|scripts/|script/|.*lock.*|\.specify/)
  ```

---

### 4. Split MCP Import Fixes (`adguard-home-agent`, `archivebox-api`, `atlassian-agent`, `audio-transcriber`, `container-manager-mcp`)

Restore standard imports in the split `mcp/` directories to resolve `F821 Undefined name` check failures.

#### [MODIFY] [adguard-home-agent](file:///home/apps/workspace/agent-packages/agents/adguard-home-agent/adguard_home_agent/mcp/mcp_rewrites.py)
- Ensure the following imports are present at the top of the file:
  ```python
  import logging
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from adguard_home_agent.adguard_home import AdGuardHome
  from adguard_home_agent.mcp.mcp_misc import ctx_log, ctx_progress, ctx_confirm_destructive
  ```

#### [MODIFY] [archivebox-api](file:///home/apps/workspace/agent-packages/agents/archivebox-api/archivebox_api/mcp/mcp_box.py)
- Ensure the following imports are present at the top of the file:
  ```python
  import logging
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from archivebox_api.archivebox import ArchiveBox
  from archivebox_api.mcp.mcp_misc import ctx_log
  ```

#### [MODIFY] [atlassian-agent](file:///home/apps/workspace/agent-packages/agents/atlassian-agent/atlassian_agent/mcp/mcp_jira.py)
- Ensure the following imports are present at the top of the file:
  ```python
  import logging
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from atlassian_agent.jira import JiraClient
  from atlassian_agent.mcp.mcp_misc import ctx_log, ctx_progress, ctx_confirm_destructive
  ```

#### [MODIFY] [audio-transcriber](file:///home/apps/workspace/agent-packages/agents/audio-transcriber/audio_transcriber/mcp/mcp_audio_processing.py)
- Ensure the following imports are present at the top of the file:
  ```python
  import logging
  from pathlib import Path
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from audio_transcriber.audio_transcriber import AudioTranscriber
  from audio_transcriber.mcp.mcp_misc import (
      ctx_log,
      DEFAULT_TRANSCRIBE_DIRECTORY,
      DEFAULT_WHISPER_MODEL,
      logger,
  )
  ```

#### [MODIFY] [container-manager-mcp](file:///home/apps/workspace/agent-packages/agents/container-manager-mcp/container_manager_mcp/mcp/mcp_compose.py)
- Add missing imports:
  ```python
  import os
  import logging
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from container_manager_mcp.manager import create_manager
  from container_manager_mcp.mcp.mcp_misc import ctx_log
  ```

#### [MODIFY] [container-manager-mcp](file:///home/apps/workspace/agent-packages/agents/container-manager-mcp/container_manager_mcp/mcp/mcp_container.py)
- Add missing imports:
  ```python
  import os
  import logging
  from typing import Literal
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from container_manager_mcp.manager import create_manager
  from container_manager_mcp.mcp.mcp_misc import ctx_log, ctx_confirm_destructive, ctx_progress
  ```

#### [MODIFY] [container-manager-mcp](file:///home/apps/workspace/agent-packages/agents/container-manager-mcp/container_manager_mcp/mcp/mcp_image.py)
- Add missing imports:
  ```python
  import os
  import logging
  from typing import Literal
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from container_manager_mcp.manager import create_manager
  from container_manager_mcp.mcp.mcp_misc import ctx_log, ctx_confirm_destructive, ctx_progress
  ```

#### [MODIFY] [container-manager-mcp](file:///home/apps/workspace/agent-packages/agents/container-manager-mcp/container_manager_mcp/mcp/mcp_info.py)
- Add missing imports:
  ```python
  import os
  import logging
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from container_manager_mcp.manager import create_manager
  from container_manager_mcp.mcp.mcp_misc import ctx_log
  ```

#### [MODIFY] [container-manager-mcp](file:///home/apps/workspace/agent-packages/agents/container-manager-mcp/container_manager_mcp/mcp/mcp_network.py)
- Add missing imports:
  ```python
  import os
  import logging
  from typing import Literal
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from container_manager_mcp.manager import create_manager
  from container_manager_mcp.mcp.mcp_misc import ctx_log, ctx_confirm_destructive, ctx_progress
  ```

#### [MODIFY] [container-manager-mcp](file:///home/apps/workspace/agent-packages/agents/container-manager-mcp/container_manager_mcp/mcp/mcp_volume.py)
- Add missing imports:
  ```python
  import os
  import logging
  from typing import Literal
  from mcp.server.fastmcp import Context, FastMCP
  from pydantic import Field
  from container_manager_mcp.manager import create_manager
  from container_manager_mcp.mcp.mcp_misc import ctx_log, ctx_confirm_destructive, ctx_progress
  ```

---

## Verification Plan

### Automated Verification
1. Run pre-commit hooks locally inside each target package repository using `uv run pre-commit run --all-files` to ensure instant validation.
2. Once the local runs are successful, trigger a full validation sweep using:
   ```bash
   mcp_rm_projects validate type=all generate_report=true output_dir=/home/apps/workspace/reports
   ```
3. Read the validation job status via `validate_status` to ensure **100% compliance (0 errors across all 43 repositories)**.
