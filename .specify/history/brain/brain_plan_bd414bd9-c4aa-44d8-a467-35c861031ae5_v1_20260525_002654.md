# Implementation Plan - Driving Code Enhancement for `servicenow-api`

This implementation plan details the steps required to achieve 100% lint compliance, resolve all pytest quality/assertion warnings, ensure full Concept Traceability mapping, update README.md and documentation references, and clean up the test suite.

---

## User Review Required

> [!IMPORTANT]
> - **Redundant/Placeholder Test Deletion**: We propose deleting the generic placeholder test `tests/test_placeholder.py` as the test suite is now extremely comprehensive.
> - **Doc-Test / Type Assertions**: We will fix the type comparison assertions in `tests/test_api_brute_force.py`, `tests/test_api_wrapper.py`, and `tests/test_servicenow_api_brute_force_coverage.py` to use `is` / `isinstance()` instead of `==`, perfectly aligning with ruff `E721` requirements.
> - **Bare Exception Handling**: We will convert a bare `except` in `tests/test_servicenow_api_brute_force_coverage.py` into a safe, explicit `except Exception:`.

---

## Proposed Changes

### 1. Code Hygiene & Lint Fixes

#### [MODIFY] [test_api_brute_force.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_api_brute_force.py)
*   Fix `E721` type comparison warnings by changing `== bool` to `is bool`, `== int` to `is int`, and `== list` to `is list`.
*   Inject `CONCEPT:ECO-4.0` in the module docstring for Tool Interface & MCP tracking.

#### [MODIFY] [test_api_massive_coverage.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_api_massive_coverage.py)
*   Change generic blind exception assert `pytest.raises(Exception)` to the specific `pytest.raises(ValueError)` boundary exception check.
*   Rename the unused loop control variable `name` in line 374 to `_name`.
*   Add `CONCEPT:ECO-4.0` in the test function docstring to track Tool Interface & MCP.

#### [MODIFY] [test_api_wrapper.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_api_wrapper.py)
*   Fix `E721` type check in line 34: change `assert table == list` to `assert table is list`.

#### [MODIFY] [test_servicenow_api_brute_force_coverage.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_servicenow_api_brute_force_coverage.py)
*   Change bare `except` in line 47 to `except Exception:`.
*   Remove repeated dictionary key `"className"` in line 70.
*   Fix `E721` type check: change `== int` to `is int` and `== bool` to `is bool`.
*   Add explicit robust assertions to `test_servicenow_api_brute_force` and `test_mcp_server_coverage` to resolve the "no assertions" pytest warning.

#### [MODIFY] [test_servicenow_models.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_servicenow_models.py)
*   Remove unused imports `typing.Any` and `servicenow_api`.
*   Change the generic exception raise to a standard `ImportError` with proper syntax (`raise ImportError("ERROR IMPORTING")`).

#### [DELETE] [test_placeholder.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_placeholder.py)
*   Completely delete this file as it is a redundant placeholder.

---

### 2. Test Suite Assertion Quality & Concept Traceability

#### [MODIFY] [test_startup.py](file:///home/apps/workspace/agent-packages/agents/servicenow-api/tests/test_startup.py)
*   Add explicit assertions testing package exports, properties, and version details.
*   Inject `CONCEPT:OS-5.0` to track Agent OS Kernel startup behavior.

#### [MODIFY] [All source and test files]
*   Inject explicit, verified `CONCEPT:ID` tags into all source and test modules to reach 100% Concept Traceability mapping.
    - `servicenow_api/__init__.py`: `CONCEPT:OS-5.0`
    - `servicenow_api/agent_server.py`: `CONCEPT:ORCH-1.3`
    - `servicenow_api/api_client.py`: `CONCEPT:ECO-4.0`
    - `servicenow_api/auth.py`: `CONCEPT:OS-5.1`
    - `servicenow_api/mcp_server.py`: `CONCEPT:ECO-4.0`, `CONCEPT:OS-5.1`
    - `servicenow_api/servicenow_models.py`: `CONCEPT:ECO-4.0`
    - Test files: corresponding mapping to `CONCEPT:ECO-4.0`, `CONCEPT:OS-5.0`, `CONCEPT:OS-5.1`.

---

### 3. Documentation & Environment Variables

#### [MODIFY] [README.md](file:///home/apps/workspace/agent-packages/agents/servicenow-api/README.md)
*   Add a visual Table of Contents at the top.
*   Add direct hyperlinks pointing to key `/docs` files (`docs/index.md`, etc.).
*   Add a comprehensive **Environment Variables** section documenting all active env vars (`SERVICENOW_INSTANCE`, `SERVICENOW_USERNAME`, `SERVICENOW_PASSWORD`, `ENABLE_OTEL`, `OTEL_EXPORTER_OTLP_ENDPOINT`, etc.).

#### [MODIFY] [.env.example](file:///home/apps/workspace/agent-packages/agents/servicenow-api/.env.example)
*   Document every environment variable referenced in the agent server, MCP server, and `agent-utilities` library, including LLM keys, Eunomia URLs, OAuth credentials, and host configs.

#### [MODIFY] [docs/index.md](file:///home/apps/workspace/agent-packages/agents/servicenow-api/docs/index.md)
*   Ensure we list the architecture, MCP to API mapping, and setup guides clearly.

#### [MODIFY] [CHANGELOG.md](file:///home/apps/workspace/agent-packages/agents/servicenow-api/CHANGELOG.md)
*   Format the changelog to fully comply with Keep a Changelog.
*   Add an entry for the current version `1.19.0` detailing changes made.

---

## Verification Plan

### Automated Tests
*   Run the main pytest suite to verify all tests remain green:
    ```bash
    uv run pytest
    ```
*   Verify that ruff passes with zero errors:
    ```bash
    uv run ruff check .
    ```

### Manual Verification
*   Inspect the newly generated documentation files to ensure they look premium and compile correctly.
