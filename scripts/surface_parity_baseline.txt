# Surface-parity baseline — capability modules not yet reachable from the
# gateway/MCP operator surface (AGENTS.md "Two surfaces by default").
#
# RATCHET AT ZERO. Every capability module is now reachable on BOTH surfaces.
# The gate (scripts/check_surface_parity.py) fails on ANY new unexposed module —
# so a newly-added feature that isn't wired to gateway + MCP is a build break.
# Keep this file empty; do not add entries to silence the gate.
