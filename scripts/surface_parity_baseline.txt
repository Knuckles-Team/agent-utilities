# Surface-parity baseline — capability modules the static scan reports as not
# reachable from the gateway/MCP operator surface (AGENTS.md "Two surfaces by
# default"). The gate (scripts/check_surface_parity.py) fails only on NEW entries,
# so this file records ACKNOWLEDGED, verified exceptions — not "silence the gate".
#
# Each entry below is a STATIC-IMPORT FALSE POSITIVE, not a half-shipped feature.
# Do not add a module here to dodge wiring it; add it only after verifying (as
# below) that it is genuinely reached on a live surface path via dynamic import.
#
# ──────────────────────────────────────────────────────────────────────────────
# domains/finance/* — internal quant compute, reached via LAZY import (KG-2.6)
# ──────────────────────────────────────────────────────────────────────────────
# Root cause: commit e32d2f09 ("build(serving): slim the serving image") — part of
# the torch-free / [finance]-extra-guard work (958f39e5, 83918ae9) — rewrote
# agent_utilities/domains/finance/__init__.py from eager `from .alpha_factors
# import (...)` lines into a lazy `__getattr__` + `_SYMBOL_MODULES` registry that
# resolves each symbol with `importlib.import_module()` on first attribute access.
# That keeps the lean serving image from dragging scipy/pandas/statsmodels onto
# every import path, but it SEVERS the static `import` edge that
# scripts/check_wiring.build_graph (a pure-AST import-graph walk) relies on.
# Runtime reachability is UNCHANGED — the surface still reaches these modules.
#
# These are NOT user-facing features that each need their own graph_* action. Per
# AGENTS.md "Sprawl boundaries", finance/quant/exchange compute is INTERNAL compute
# that stays light in core (heavy weight re-homed to emerald-exchange / data-
# science-mcp, reached over MCP). The operator surface for the finance domain is
# the single `quant` MCP tool + its `/quant` REST twin (registered on a live
# surface path: kg_server -> mcp/tools/ontology_tools.py ->
# domains/finance/quant_mcp_tools.register_quant_tools; tool<->route parity is
# GREEN), plus the finance `DOMAIN_REGISTRY` (domains/__init__.py) and the service
# registry (core/registry/service_adapter.py) — every one of which reaches these
# modules through string/`importlib` paths the static scanner cannot follow.
# Wiring each as a second per-module handler would create the "second
# implementation" the Two-Surfaces contract explicitly forbids. So: baseline, do
# not guess-wire.
#
# Verified-reached-on-a-surface (lazy `__getattr__` in finance/__init__.py and/or
# the DOMAIN_REGISTRY / service_adapter descriptor strings):
agent_utilities/domains/finance/alpha_factors.py
agent_utilities/domains/finance/composite_backtest.py
agent_utilities/domains/finance/copy_trade.py
agent_utilities/domains/finance/credit_quality.py
agent_utilities/domains/finance/engine_series.py
agent_utilities/domains/finance/execution.py
agent_utilities/domains/finance/features.py
agent_utilities/domains/finance/filing_diff.py
agent_utilities/domains/finance/geopolitical_risk.py
agent_utilities/domains/finance/kronos_forecaster.py
agent_utilities/domains/finance/market_feeds.py
agent_utilities/domains/finance/pattern_classifier.py
agent_utilities/domains/finance/payments.py
agent_utilities/domains/finance/portfolio_optimizer.py
agent_utilities/domains/finance/profit_attribution.py
agent_utilities/domains/finance/research_autopilot.py
agent_utilities/domains/finance/risk_manager.py
agent_utilities/domains/finance/sentiment_fusion.py
# signal_fusion: statically imported by sentiment_fusion (BayesianSignalFusion),
# which is itself reached only via the lazy registry above — same severed chain.
agent_utilities/domains/finance/signal_fusion.py
agent_utilities/domains/finance/strategy_export.py
agent_utilities/domains/finance/strategy_sharing.py
agent_utilities/domains/finance/streaming.py
agent_utilities/domains/finance/trade_journal.py
agent_utilities/domains/finance/versioned_orders.py
agent_utilities/domains/finance/visual_ta.py
# ──────────────────────────────────────────────────────────────────────────────
# knowledge_graph/retrieval/*_context.py — context-plane providers, reached via
# DYNAMIC import (KG-2.136 universal context plane)
# ──────────────────────────────────────────────────────────────────────────────
# These are the built-in providers of the context plane
# (retrieval/context_plane.py): each is registered in ``_BUILTIN_PROVIDERS`` as a
# ``(module_path, fn_name)`` string pair and resolved with ``importlib.import_module()``
# on first use — explicitly so the plane carries "no static dependency on heavy
# provider modules". The operator surface for them is the ONE context plane,
# reached live via ``graph_analyze action=explain`` / ``code_context``
# (kg_server -> graph_analyze -> context_plane.synthesize_context -> provider), not
# a per-provider graph_* tool (which would be the forbidden second implementation).
# Runtime reachability is real; only the static import edge is severed.
agent_utilities/knowledge_graph/retrieval/deploy_context.py
agent_utilities/knowledge_graph/retrieval/entity_context.py
agent_utilities/knowledge_graph/retrieval/ops_context.py
