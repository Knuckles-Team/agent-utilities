# (file, pattern) pairs already reviewed and confirmed NOT a live credential.
# Generated by manual review, lane burn-ci-0802, 2026-08-01, against the full
# origin/main..HEAD range (237,225 added lines). Format: <file>\t<pattern>
# per line. D-CIP-14 ratchet: a pair here never blocks; anything NOT here
# fails closed. Keyed on (file, pattern), not commit -- the scanned range
# grows with every push, so this stays valid without updating on every run.
#
# Fix or retire an entry by adding an inline '# sanitizer:ignore' marker to
# the offending line instead (the stronger, self-documenting convention used
# elsewhere in this repo's test suite) and removing the row here; re-run
# --base origin/main to confirm removal doesn't reintroduce a hit.

# Deployment wizard's own documented CHANGEME placeholder default, not a
# real password -- matches check_wheel_privacy.py's placeholder allowance.
b/agent_utilities/skills/skill_graphs/agent-utilities/deployment/scripts/deploy_wizard.py	db_url_with_password

# tests/gates/test_wheel_privacy_gate.py IS the proof-of-catch suite for the
# wheel-privacy gate (D-CIP-15/D-ORC-23) -- it deliberately contains
# synthetic credential-shaped fixtures (a toy PEM block, a fake API key, the
# documented postgresql://agent:agent@... placeholder default) to prove that
# gate catches them. Same class this gate itself uses in its own --self-check.
b/tests/gates/test_wheel_privacy_gate.py	db_url_with_password
b/tests/gates/test_wheel_privacy_gate.py	generic_secret_assignment
b/tests/gates/test_wheel_privacy_gate.py	private_key_block

# Work-item claim tokens (wi.claim_specific(..., token="racer-mid", ...)) --
# the regex's generic `token\s*[:=]\s*['"]...['"]` shape matches these, but
# they are in-memory work-queue lease identifiers, not credentials.
b/tests/scale/soak/test_chaos_connector_and_fencing.py	generic_secret_assignment
b/tests/unit/orchestration/test_work_item.py	generic_secret_assignment

# "AKIAABCDEFGHIJKLMNOP" is this repo's own established synthetic  # sanitizer:ignore - documentation prose quoting the repo's synthetic fixture literal, not a live key
# AWS-key-shaped fixture (same literal string used in
# tests/gates/test_wheel_privacy_gate.py and the original D-CIP-13 scan's
# findings) -- asserted here as text that must NOT leak into guardrail output.
b/tests/unit/capabilities/test_content_guardrails.py	aws_access_key_id

# Synthetic "sk-proj-..." fixtures asserting the error/redaction surface
# never echoes a secret-shaped value back to a caller.
b/tests/unit/core/test_openai_catalog.py	openai_project_key
b/tests/unit/mcp/test_graph_observe_error_detail.py	openai_project_key
b/tests/unit/security/test_error_surface.py	openai_project_key
b/tests/unit/security/test_error_surface.py	basic_auth_url

# "sk-should-never-appear-in-report" -- self-describing synthetic fixture for
# the doctor/openai-catalog redaction test.
b/tests/unit/deployment/test_doctor_openai_catalog.py	generic_secret_assignment

# "fake-test-token" -- self-describing synthetic fixture for the
# OpenBao-signing custody test.
b/tests/unit/security/test_openbao_signing_custody.py	generic_secret_assignment

# `token="hostB:9:agent-dispatch"` -- an in-process dispatch-worker identity
# string in a unit test, not a credential.
b/tests/unit/test_agent_dispatch.py	generic_secret_assignment

# `token="dead-worker"` (wi.claim_specific(...)) -- a synthetic in-process
# worker-lease claim token used to exercise ADR-6 lease-expiry re-queue
# behaviour in a unit test, not a credential. Same class as the
# tests/unit/test_agent_dispatch.py entry above (a work-item lease/claim
# identity string, not a secret) -- no rotation required. GOC-59-W08/B1.
b/tests/unit/orchestration/test_agent_activation.py	generic_secret_assignment
