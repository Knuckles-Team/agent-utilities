# ═══════════════════════════════════════════════════════════════════════════════
# agent-utilities — Environment Configuration Template
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and configure your values.
#
# IMPORTANT: All LLM model registries (models, specific base URLs, API keys)
# should be managed centrally via the XDG-compliant configuration file at:
#   ~/.config/agent-utilities/config.json
#
# Environment variables defined in this file act as secondary overrides for all
# system settings. List or dictionary variables can be passed as JSON strings.
# ═══════════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────────
#  ⚠️  DEPRECATED / LEGACY LLM SETTINGS (DO NOT USE)
# ───────────────────────────────────────────────────────────────────────────────
# These legacy variables are deprecated and will be ignored in favor of the XDG
# config.json registries (chat_models / embedding_models):
#
# LLM_BASE_URL=http://localhost:1234/v1
# LLM_API_KEY=your-api-key-here
# MODEL_ID=openai:gpt-4o
# PROVIDER=openai
# AGENT_WORKSPACE=.                            # Replaced by WORKSPACE_PATH
# MCP_CONFIG_PATH=mcp_config.json              # Replaced by MCP_CONFIG


# ───────────────────────────────────────────────────────────────────────────────
#  1. Primary Model Registry (JSON Overrides)
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] JSON string array of chat models. Overrides config.json registries.
# CHAT_MODELS='[{"id":"qwen/qwen3.5-9b","provider":"openai","base_url":"http://vllm-embed.arpa/v1","supports_json":false,"vision":true,"reasoning":true,"tools_enabled":true,"parallel_instances":3,"context_window":256000,"intelligence_level":"normal","can_route":true,"can_kg":true}]'

# [OPTIONAL] JSON string array of embedding models. Overrides config.json registries.
# EMBEDDING_MODELS='[{"id":"text-embedding-nomic-embed-text-v2-moe","provider":"openai","base_url":"http://vllm-embed.arpa/v1","parallel_instances":4,"chunk_size":768}]'


# ───────────────────────────────────────────────────────────────────────────────
#  2. Global Provider API Keys (Fallbacks)
# ───────────────────────────────────────────────────────────────────────────────
# Set key/endpoint overrides for providers if they are not defined in config.json:
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=AIzaSy...
# GROQ_API_KEY=gsk_...
# MISTRAL_API_KEY=...
# HUGGING_FACE_API_KEY=hf_...
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1


# ───────────────────────────────────────────────────────────────────────────────
#  3. Agent Identity & Workspaces
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Agent display identity
# DEFAULT_AGENT_NAME=Agent
# AGENT_DESCRIPTION="AI Agent"
# AGENT_SYSTEM_PROMPT=

# [OPTIONAL] System workspace path (where filesystem and repository tools execute)
# WORKSPACE_PATH=/home/apps/workspace

# [OPTIONAL] Custom XDG configuration root path
# AGENT_UTILITIES_CONFIG_DIR=~/.config/agent-utilities


# ───────────────────────────────────────────────────────────────────────────────
#  4. Server Network Settings
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Core protocol server network settings
# HOST=0.0.0.0
# PORT=9000
# DEBUG=false

# [OPTIONAL] UI Toggles
# ENABLE_WEB_UI=false
# ENABLE_TERMINAL_UI=false
# ENABLE_WEB_LOGS=true

# [OPTIONAL] Agent Control Protocol (ACP) parameters
# ENABLE_ACP=false
# ACP_PORT=8001
# ACP_SESSION_ROOT=.acp-sessions
# DEFAULT_TERMINAL_AGENT=agent-terminal-ui


# ───────────────────────────────────────────────────────────────────────────────
#  5. Security & Authentication
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] API basic authorization and file transfer controls
# ENABLE_API_AUTH=false
# AGENT_API_KEY=my-secure-api-key
# MAX_UPLOAD_SIZE=10485760                     # Limit upload size in bytes (10MB default)

# [OPTIONAL] OIDC / JWT Token Verification
# AUTH_JWT_JWKS_URI=https://identity.provider/keys
# AUTH_JWT_ISSUER=https://identity.provider
# AUTH_JWT_AUDIENCE=my-agent-client-id

# [OPTIONAL] OIDC Client Delegation (RFC 8693 Token Exchange)
# OIDC_CONFIG_URL=https://identity.provider/.well-known/openid-configuration
# OIDC_CLIENT_ID=client-id
# OIDC_CLIENT_SECRET=client-secret
# ENABLE_DELEGATION=false
# AUDIENCE=https://downstream.api/v1           # Target audience for exchange
# DELEGATED_SCOPES=api                         # Space-separated scopes

# [OPTIONAL] CORS & Host Security
# ALLOWED_ORIGINS=*                            # Comma-separated CORS origins
# ALLOWED_HOSTS=*                              # Comma-separated trusted hosts


# ───────────────────────────────────────────────────────────────────────────────
#  6. Secrets Storage Backend
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Secret backends (inmemory, sqlite, vault)
# SECRETS_BACKEND=inmemory
# SECRETS_SQLITE_PATH=~/.agent-utilities/secrets.db
# SECRETS_VAULT_URL=http://127.0.0.1:8200
# SECRETS_VAULT_MOUNT=secret

# [OPTIONAL] Vault / OpenBao Auth & Settings (used with 'vault' backend)
# VAULT_AUTH_METHOD=auto                       # auto | oidc | approle | token | kubernetes
# VAULT_AUTH_MOUNT=jwt                         # Auth mount path
# VAULT_ROLE=default                           # Login role name
# VAULT_PATH_PREFIX=agents/mcp/                # KV v2 path prefix
# AGENT_SECRETS_FILE=                          # Path to custom JSON secrets file


# ───────────────────────────────────────────────────────────────────────────────
#  7. Graph Orchestration & Persistence
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Knowledge-graph backend selection.
# Out-of-box default (when unset): "epistemic_graph" — the engine IS the one
# database (compute + cache + semantic + durable persistence), a self-contained
# binary with NO external dependencies (no Postgres/Neo4j server needed).
# GRAPH_BACKEND=epistemic_graph                # epistemic_graph | fanout | memory | file | postgresql | age
# To add MIRRORS (interop/BI/DR) — the engine stays the authority serving reads,
# writes fan out losslessly to the named mirrors — set "fanout" + targets:
# GRAPH_BACKEND=fanout
# GRAPH_AUTHORITY=epistemic_graph              # source-of-truth (default: the engine)
# GRAPH_MIRROR_TARGETS=age                     # CSV/JSON of mirror connection names (e.g. age,neo4j,falkordb)
# GRAPH_DB_URI=postgresql://agent:agent@localhost:5432/agent_kg   # DSN for the pg-age mirror
# GRAPH_DB_PATH=~/.local/share/agent-utilities/kg/knowledge_graph.db   # LadybugDB file path
# Multi-SoR asset mirror — project the KG's :Asset/CI model out to CMDB sinks.
# Empty by default (every sink inert). List a subset of servicenow,erpnext,egeria,twenty.
# Each listed sink still needs its own <SINK>_ENABLE_WRITE for live writes (dry-run-first).
# ASSET_MIRROR_TARGETS=servicenow,egeria       # CSV/JSON of enabled CMDB sinks
# SERVICENOW_ENABLE_WRITE=false                # gate live writes per sink (fail-closed)
# ERPNEXT_ENABLE_WRITE=false
# EGERIA_ENABLE_WRITE=false
# TWENTY_ENABLE_WRITE=false
# [OPTIONAL] Graph Orchestration routing and validations
# ROUTING_STRATEGY=hybrid                      # hybrid | semantic | rule-based
# GRAPH_PERSISTENCE_TYPE=file                  # file | memory
# GRAPH_PERSISTENCE_PATH=~/.local/share/agent-utilities/graph_state
# ENABLE_LLM_VALIDATION=false                  # Validate outputs via additional LLM passes
# GRAPH_ROUTER_TIMEOUT=300.0                   # Router maximum timeout in seconds
# GRAPH_VERIFIER_TIMEOUT=300.0                 # Verifier maximum timeout in seconds
# GRAPH_DIRECT_EXECUTION=true                  # Direct graph dispatch (bypasses LLM tool call hop)
# MIN_CONFIDENCE=0.4                           # Routing decision confidence threshold
# VALIDATION_MODE=false                        # Mock LLM generation for CI/testing
# APPROVAL_TIMEOUT=0.0                         # Seconds to wait for manual human approval (0 = block)


# ───────────────────────────────────────────────────────────────────────────────
#  8. Knowledge Graph Ingestion & Tuning
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Graph timeouts and structural constraints
# GRAPH_TIMEOUT=1200000                        # Global network/query timeout in ms
# MAX_RECURSION_DEPTH=2                        # Maximum graph expansion depth
# ROUTING_PERCENTILE=50.0                      # Semantic routing percentile limit
# KG_EMBEDDING_DIM=768                         # Knowledge Graph embedding dimension size

# [OPTIONAL] Knowledge Graph Sync and Concurrency
# ENABLE_KG_EMBEDDINGS=true
# KG_BACKUPS=3                                 # Keep N timestamped SQLite/DuckDB backups
# KG_INGESTION_WORKERS=                        # Max worker threads for codebase ingestion
# KG_LLM_CONCURRENCY=4                         # Max concurrent LLM calls for KG analysis
# KG_ANALYSIS_MAX_DEPTH=2                      # Ingestion graph analyzer recursion depth
# KNOWLEDGE_GRAPH_SYNC_BACKGROUND=true        # Sync knowledge graph in the background
# MODEL_REGISTRY_PATH=                         # Path to models.yaml registry


# ───────────────────────────────────────────────────────────────────────────────
#  9. Observability & Telemetry
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] OpenTelemetry (OTLP) Exports
# ENABLE_OTEL=false
# OTEL_EXPORTER_OTLP_ENDPOINT=http://langfuse.arpa/api/public/otel
# OTEL_EXPORTER_OTLP_HEADERS=
# OTEL_EXPORTER_OTLP_PUBLIC_KEY=lf_pk_...
# OTEL_EXPORTER_OTLP_SECRET_KEY=lf_sk_...
# OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf

# [OPTIONAL] Langfuse Observability Integration
# LANGFUSE_PUBLIC_KEY=lf_pk_...
# LANGFUSE_SECRET_KEY=lf_sk_...
# LANGFUSE_URL=http://langfuse.arpa
# LANGFUSE_DATASET_CAPTURE_THRESHOLD=0.0       # Capture score threshold for dataset creation
# LANGFUSE_LATENCY_BASELINE_SECONDS=60.0       # Alert baseline latency
# LANGFUSE_TOKEN_BASELINE=20000                # Expected token limit warning threshold
# LANGFUSE_VERIFIER_FALLBACK_LIMIT=1           # Retries before verification fallback

# [OPTIONAL] Self-ingest telemetry (CONCEPT:KG-2.304 / AU-OS.observability.durable-telemetry-pipeline) — dogfooding
# Ship agent-utilities + graph-os's OWN logs (and RunTrace / ToolCall provenance)
# INTO the epistemic-graph engine's observability store over its OTLP/HTTP log
# endpoint (engine CONCEPT:EG-160). Opt-in + default-off: nothing is shipped
# unless AGENT_UTILITIES_SELF_INGEST is truthy AND EPISTEMIC_GRAPH_OBS_ADDR is set.
# Non-blocking (background batching thread). Once enabled the pipeline is
# durable + non-lossy by default: a failed send is requeued (bounded retries
# + backoff), backpressure/exhausted retries spill to a durable sqlite (WAL)
# buffer instead of vanishing, and every record is stamped with the ambient
# tenant/actor. The buffer itself being unavailable/at its bound is the one
# remaining loss case — always counted + logged loudly, never silent.
# AGENT_UTILITIES_SELF_INGEST=false            # Master opt-in switch (default off)
# EPISTEMIC_GRAPH_OBS_ADDR=http://obs.arpa:4318 # Engine obs endpoint base addr
# AGENT_UTILITIES_SELF_INGEST_MODE=otlp        # otlp → POST /v1/logs ; bulk → POST /_bulk
# AGENT_UTILITIES_SELF_INGEST_SERVICE=agent-utilities  # OTLP service.name
# AGENT_UTILITIES_SELF_INGEST_LEVEL=INFO       # Minimum log level to ship
# AGENT_UTILITIES_SELF_INGEST_BATCH=100        # Max records per batch
# AGENT_UTILITIES_SELF_INGEST_INTERVAL=2.0     # Background flush interval (seconds)
# AGENT_UTILITIES_SELF_INGEST_QUEUE_MAX=10000  # Bounded queue; overflow spills durably
# AGENT_UTILITIES_SELF_INGEST_TIMEOUT=3.0      # Per-request HTTP timeout (seconds)
# AGENT_UTILITIES_SELF_INGEST_MAX_RETRIES=3    # Resends before a batch spills durably
# AGENT_UTILITIES_SELF_INGEST_SPILL_PATH=      # Durable overflow sqlite path (default: XDG data dir)
# AGENT_UTILITIES_SELF_INGEST_SPILL_MAX=50000  # Durable buffer cap; beyond this = true drop

# [OPTIONAL] Fleet events webhook ingress (CONCEPT:OS-5.15)
# POST /api/fleet/events accepts Alertmanager v4 / Uptime Kuma / generic JSON
# webhooks, persists them as FleetEvent KG nodes and queues fleet_event_triage
# tasks. Alertmanager/Kuma can't mint JWTs, so an optional shared secret gates
# the endpoint instead. Unset = no token required.
# FLEET_EVENTS_TOKEN=

# [OPTIONAL] Autonomous evolution (docs/guides/autonomous-evolution.md)
# The daemon ticks are registered but OFF by default in code — turning a fleet
# autonomous is a DEPLOYMENT decision, made here, not a code default. The
# safety chain stays propose-only end-to-end unless auto-merge is also enabled:
# propose-only loops → governed validation (AHE-3.20) → regression gate → human merge.
# KG_LOOP=true                                 # Hourly propose-only research/self-evolution cycle
# KG_LOOP_INTERVAL=3600
# KG_LOOP_TOPICS=5
# KG_FAILURE_EVOLUTION=true                    # Langfuse failures → failure_gap topics → remediation proposals
# KG_FAILURE_EVOLUTION_INTERVAL=3600
# KG_ANOMALY_CONSUMER=true                     # Consume PerformanceAnomaly nodes → failure_gap topics (cheap, propose-only; default on)
# Governed auto-merge: proposal→active promotion gated by the production
# PromotionGovernanceValidator (SHACL + recorded regression gate + MergePolicy
# + constitution rules). Keep FALSE until you trust the proposal stream.
# KG_GOLDEN_AUTO_MERGE=false
# KG_GOLDEN_MERGE_THRESHOLD=0.85

# [OPTIONAL] Fleet autonomy control plane (docs/architecture/fleet_autonomy.md)
# Operational autonomy is policy-gated, never flag-only: every mutating action
# (restart/scale/deploy/rollback) passes the OS-5.24 ActionPolicy decision
# point. The shipped default policy keeps EVERYTHING mutating approval-required
# (queue at GET /api/fleet/approvals); relax per-rule via your own policy YAML
# or KG governance_rule overrides.
# ACTION_POLICY_PATH=                          # Empty = shipped conservative deploy/action-policy.default.yml
# FLEET_RECONCILER=true                        # Leader-only desired-state reconciler tick (OS-5.25; default off)
# FLEET_RECONCILER_INTERVAL=120
# FLEET_RECONCILER_MAX_ACTIONS=5               # Storm guard per tick
# FLEET_REGISTRY_PATH=                         # Empty = deploy/mcp-fleet.registry.yml
# FLEET_DESIRED_STATE_PATH=                    # Optional per-service replicas/desired/version override YAML
# FLEET_ACTUATOR=dryrun                        # dryrun (records intent only) | docker; Portainer wired via set_fleet_actuator()
# DEPLOY_WATCH_WINDOW=300                      # Health watch after deploy/restart (OS-5.27)
# DEPLOY_WATCH_POLL=15
# FLEET_AUTOSCALER=true                        # Leader-only reactive replica autoscaler tick (OS-5.29; default off)
# FLEET_AUTOSCALER_INTERVAL=60
# SCALING_PROMETHEUS_URL=                      # Empty = zero-infra in-process gauges; set http://prometheus:9090 for PromQL signals


# ───────────────────────────────────────────────────────────────────────────────
#  10. Agent-to-Agent (A2A) Discovery
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Standardized A2A communication parameters
# A2A_BROKER=in-memory
# A2A_BROKER_URL=
# A2A_STORAGE=in-memory
# A2A_STORAGE_URL=
# A2A_CONFIG=                                  # Path to external a2a_config.json
# A2A_REFRESH_INTERVAL=300                     # Period (sec) for card refresh


# ───────────────────────────────────────────────────────────────────────────────
#  11. Specialist Skills
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Loader paths for agent capabilities
# CUSTOM_SKILLS_DIRECTORY=                     # Custom skill packages folder
# SKILL_TYPES=                                 # Comma-separated list of active skill types

# [OPTIONAL] Optional-infra tool gates (default OFF — these toolsets need
# external services/credentials; opt in explicitly, see tools/tool_registry.py)
# X_TOOLS=0                                    # X/Grok social search via xAI (needs XAI_API_KEY); set 1 for production X use
# MEDIA_TOOLS=0                                # Media generation/transcription services
# DB_TOOLS=0                                   # Native database traversal tools


# ───────────────────────────────────────────────────────────────────────────────
#  12. LLM Inference Parameters
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Global inference controls and parameters
# MAX_TOKENS=16384
# TEMPERATURE=0.7
# TOP_P=1.0
# TIMEOUT=32400.0
# TOOL_TIMEOUT=32400.0
# PARALLEL_TOOL_CALLS=true
# SEED=
# PRESENCE_PENALTY=0.0
# FREQUENCY_PENALTY=0.0

# [OPTIONAL] LLM formatting controls (JSON format string parameters)
# LOGIT_BIAS=                                  # e.g., '{"5028": -100}'
# STOP_SEQUENCES=                              # e.g., '["\n", "Human:"]'
# EXTRA_HEADERS=                               # e.g., '{"X-Custom": "Val"}'
# EXTRA_BODY=                                  # e.g., '{"presence_penalty": 0.2}'


# ───────────────────────────────────────────────────────────────────────────────
#  13. Cognitive Scheduler & OS Policies
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Priority scheduling and memory policies
# COGNITIVE_SCHEDULER_ENABLED=true
# MAX_CONCURRENT_AGENTS=5
# AGENT_TOKEN_QUOTA=100000
# PREEMPTION_THRESHOLD_PCT=0.85
# AGENT_POLICIES_PATH=                         # Path to agent_policies.json
# PERMISSIONS_SIGNING_KEY=                     # Signature key for agent tokens
# SPECIALIST_REGISTRY_PATH=                    # Local specialist agents catalog directory


# ───────────────────────────────────────────────────────────────────────────────
#  14. Homeostasis & Guardrails
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Homeostatic models and tool protections
# HOMEOSTATIC_DOWNGRADE_ENABLED=true           # Automatically downgrades to light model under load
# ADVERSARIAL_VERIFICATION=false               # Enable double verification checks
# MAINTENANCE_TOKEN_BUDGET=0                   # Unlimited if 0
# MAINTENANCE_PRIORITY=LOW                     # LOW | MEDIUM | HIGH
# WATCHDOG_PATTERNS='["pyproject.toml","mcp_config.json","requirements*.txt"]'

# [OPTIONAL] System safety tool protections
# TOOL_GUARD_MODE=strict                       # strict | lax | off
# SENSITIVE_TOOL_PATTERNS='[".*delete.*",".*remove.*",".*rm_.*",".*rmdir.*",".*drop.*",".*truncate.*",".*prune.*",".*kill.*",".*terminate.*",".*reboot.*",".*shutdown.*",".*install.*",".*uninstall.*",".*redeploy.*",".*bump.*",".*create.*",".*add.*",".*post.*",".*put.*",".*insert.*",".*upload.*",".*ingest.*",".*write.*",".*update.*",".*patch.*",".*set.*",".*reset.*",".*clear.*",".*revert.*",".*replace.*",".*rename.*",".*move.*",".*rotate.*",".*start.*",".*stop.*",".*restart.*",".*pause.*",".*unpause.*",".*execute.*",".*shell.*",".*run_shell.*",".*run_command.*",".*run_script.*",".*run_code.*",".*git_.*",".*clone.*",".*pull.*",".*maintain.*",".*setup.*",".*build.*",".*validate.*",".*sync.*",".*enable.*",".*disable.*",".*activate.*",".*approve.*",".*graphql.*",".*mutation.*",".*http.*",".*eval.*",".*exec.*",".*compile.*",".*socket.*",".*connect.*",".*os\\..*",".*subprocess\\..*",".*shutil\\..*"]'


# ───────────────────────────────────────────────────────────────────────────────
#  15. MCP Tool Server Connections
# ───────────────────────────────────────────────────────────────────────────────
# [OPTIONAL] Core MCP bindings
# MCP_URL=                                     # Remote MCP SSE connection URL
# MCP_CONFIG=mcp_config.json                   # Local multi-server definitions file

# ───────────────────────────────────────────────────────────────────────────────
#  16. KG Background Daemon (single consolidated daemon — CONCEPT:KG-2.8 / OS-5.9)
# ───────────────────────────────────────────────────────────────────────────────
# The KG runs ONE background daemon (queue drain + graph writer + task workers +
# maintenance scheduler + file-watch). KG_DAEMON_ROLE selects who runs it:
#   host   — run the full daemon (set on the API gateway / agent-webui, or run the
#            standalone `graph-os-daemon` process).
#   client — spawn NOTHING; enqueue work to the durable queue the host drains
#            (set on the graph-os MCP server, CLI, and one-shot scripts).
#   auto   — default: in-process consolidated daemon (standalone / dev).
# KG_DAEMON_ROLE=auto

# ═══════════════════════════════════════════════════════════════════════════════
#  17. Extended Configuration Surface (auto-classified, code-read-verified)
# ═══════════════════════════════════════════════════════════════════════════════
# The variables below are grouped by the subsystem that reads them (every entry is
# a live `setting()`/`Field(alias=...)`/dynamic-toggle read — see
# `python -m agent_utilities.mcp.check_env_var_drift`). All are [OPTIONAL] with a
# working built-in default; set only the ones your deployment needs to override.

# ───────────────────────────────────────────────────────────────────────────────
#  18. MCP Condensed-Surface Domain Toggles
# ───────────────────────────────────────────────────────────────────────────────
# AGENTTOOL=true
# ANALYSISTOOL=true
# ANALYZE_SUITETOOL=true
# BUSTOOL=true
# CAPABILITYTOOL=true
# ENGINETOOL=true
# GRAPHOS_VERBOSETOOL=true
# INTENTTOOL=true
# METATOOL=true
# ONTOLOGYTOOL=true
# OPS_CAUSALTOOL=true
# QUANTTOOL=true
# QUERYTOOL=true
# REACHTOOL=true
# SECRETTOOL=true
# STATETOOL=true
# SWETOOL=true
# WRITE_INGESTTOOL=true

# ───────────────────────────────────────────────────────────────────────────────
#  19. Tool Surface Toggles
# ───────────────────────────────────────────────────────────────────────────────
# A2A_TOOLS=true
# AGENT_KG_TOOLS=true
# BROWSER_TOOLS=true
# COMPUTER_USE_TOOLS=false
# DB_TOOLS_ALLOW_WRITE=0
# DEVELOPER_TOOLS=true
# GIT_TOOLS=true
# MEDIA_OUTPUT_DIR=
# SCHEDULER_TOOLS=true
# SWE_TOOLS=false
# WORKSPACE_TOOLS=true

# ───────────────────────────────────────────────────────────────────────────────
#  20. Messaging Platform Config
# ───────────────────────────────────────────────────────────────────────────────
# BUS_HUB_ID=
# GOOGLE_CHAT_SERVICE_ACCOUNT=
# GOOGLE_MEET_SERVICE_ACCOUNT=
# IRC_NICKNAME=agent_bot
# IRC_PORT=6667
# IRC_SERVER=
# LINE_CHANNEL_ACCESS_TOKEN=
# MATRIX_ACCESS_TOKEN=
# MATRIX_HOMESERVER=
# MATRIX_USER_ID=
# MATTERMOST_BOT_USER=
# MATTERMOST_URL=
# MESSAGING_AGENT=
# MESSAGING_ALERT_INTAKE_PORT=
# MESSAGING_BURST_MAX_S=12
# MESSAGING_BURST_WINDOW_S=2.5
# MESSAGING_CLAUDE_MODEL=claude-sonnet-4-6
# MESSAGING_CLAUDE_TRIGGER=/claude
# MESSAGING_DEFAULT_CHANNEL=
# MESSAGING_DEFAULT_PLATFORM=telegram
# MESSAGING_DISCORD_TOKEN=
# MESSAGING_ENABLED_BACKENDS=
# MESSAGING_ENRICH=1
# MESSAGING_GOALS=1
# MESSAGING_GOOGLECHAT_TOKEN=
# MESSAGING_GOOGLEMEET_TOKEN=
# MESSAGING_INBOX_RETRY_S=120
# MESSAGING_IRC_CHANNELS=
# MESSAGING_IRC_NICKNAME=agent_bot
# MESSAGING_IRC_PORT=6667
# MESSAGING_IRC_SERVER=
# MESSAGING_KG_INGEST=true
# MESSAGING_KG_MEMORY_TYPE=episodic
# MESSAGING_LINE_TOKEN=
# MESSAGING_LOCAL_MODEL=
# MESSAGING_MATRIX_HOMESERVER=
# MESSAGING_MATRIX_TOKEN=
# MESSAGING_MATRIX_USER_ID=
# MESSAGING_MATTERMOST_TOKEN=
# MESSAGING_MATTERMOST_URL=
# MESSAGING_NEXTCLOUD_APP_ID=
# MESSAGING_NEXTCLOUD_TOKEN=
# MESSAGING_NEXTCLOUD_URL=
# MESSAGING_REACTIONS=1
# MESSAGING_REPLY_TIMEOUT=45
# MESSAGING_ROUTE_TO_PLANNER=true
# MESSAGING_SIGNAL_TOKEN=
# MESSAGING_SLACK_APP_TOKEN=
# MESSAGING_SLACK_TOKEN=
# MESSAGING_SYNOLOGY_WEBHOOK_URL=
# MESSAGING_TEAMS_APP_ID=
# MESSAGING_TEAMS_APP_SECRET=
# MESSAGING_TELEGRAM_TOKEN=
# MESSAGING_TWITCH_CHANNELS=
# MESSAGING_TWITCH_TOKEN=
# MESSAGING_VOICE=1
# MESSAGING_VOICECALL_APP_ID=
# MESSAGING_VOICECALL_FROM_NUMBER=
# MESSAGING_VOICECALL_TOKEN=
# MESSAGING_VOICE_MODEL=base
# MESSAGING_WEBHOOK_BASE_URL=
# MESSAGING_WEBHOOK_PORT=8443
# MESSAGING_WEBHOOK_SECRET=
# MESSAGING_WHATSAPP_PHONE_NUMBER_ID=
# MESSAGING_WHATSAPP_TOKEN=
# MESSAGING_WHATSAPP_USE_BUSINESS_API=false
# MSTEAMS_APP_PASSWORD=
# NEXTCLOUD_TOKEN=
# NEXTCLOUD_URL=
# NEXTCLOUD_USER=
# OPENBAO_TOKEN=
# OPENBAO_URL=
# SIGNAL_PHONE_NUMBER=
# SLACK_APP_TOKEN=
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=
# TWITCH_CHANNELS=
# TWITCH_OAUTH_TOKEN=

# ───────────────────────────────────────────────────────────────────────────────
#  21. Writeback Sink Live-Write Gates
# ───────────────────────────────────────────────────────────────────────────────
# ANSIBLE_ENABLE_WRITE=false
# ARCHIMATE_ENABLE_WRITE=false
# CADDY_ENABLE_WRITE=false
# CISO_ASSISTANT_ENABLE_WRITE=false
# EMERALD_ENABLE_WRITE=false
# GITHUB_ENABLE_WRITE=false
# GITLAB_ENABLE_WRITE=false
# HOMEASSISTANT_ENABLE_WRITE=false
# JIRA_ENABLE_WRITE=false
# KEYCLOAK_ENABLE_WRITE=false
# LEANIX_ENABLE_WRITE=false
# LEGAL_ENABLE_WRITE=false
# LGTM_ENABLE_WRITE=false
# MEALIE_ENABLE_WRITE=false
# NEXTCLOUD_ENABLE_WRITE=false
# OKF_ENABLE_WRITE=false
# OKTA_ENABLE_WRITE=false
# PLANE_ENABLE_WRITE=false
# PORTAINER_ENABLE_WRITE=false
# SALESFORCE_ENABLE_WRITE=false
# TECHNITIUM_DNS_ENABLE_WRITE=false
# UPTIME_KUMA_ENABLE_WRITE=false
# WGER_ENABLE_WRITE=false

# ───────────────────────────────────────────────────────────────────────────────
#  22. External System Discovery (EA / ITSM / Observability endpoints)
# ───────────────────────────────────────────────────────────────────────────────
# BACKSTAGE_FILE=catalog-info.yaml
# BAO_URL=
# BPMN_FILE=process.bpmn
# BPM_PROVIDER=opensource
# BPM_TOKEN=
# BPM_URL=
# CADDY_API_URL=
# CADDY_URL=
# CHECKLIST_FILE=task.md
# DATABASE_PATH=:memory:
# EAR_TOKEN=
# EAR_URL=
# EMERALD_API_KEY=
# EMERALD_URL=
# ERPNEXT_TOKEN=
# ERPNEXT_URL=
# ESSENTIAL_EA_TOKEN=
# ESSENTIAL_EA_URL=
# GITHUB_API_KEY=
# GITHUB_TOKEN=
# GITLAB_API_TOKEN=
# GLPI_TOKEN=
# GLPI_URL=
# GRAFANA_URL=
# JIRA_API_TOKEN=
# JIRA_TOKEN=
# JIRA_URL=
# KEYCLOAK_ADMIN_PASSWORD=
# LGTM_URL=
# LISTMONK_TOKEN=
# LISTMONK_URL=
# MATTERMOST_TOKEN=
# NEXTCLOUD_PASSWORD=
# OPENMAINT_TOKEN=
# OPENMAINT_URL=
# PLANE_API_TOKEN=
# PLANE_TOKEN=
# PLANE_URL=
# PORTAINER_PASSWORD=
# PORTAINER_TOKEN=
# PORTAINER_URL=
# POSTIZ_TOKEN=
# POSTIZ_URL=
# SCHOLARX_API_KEY=
# SCHOLARX_URL=
# SERVICENOW_PASSWORD=
# SERVICENOW_URL=
# SERVICENOW_USER=
# TECHNITIUM_TOKEN=
# TECHNITIUM_URL=
# TUNNEL_MANAGER_URL=
# TUNNEL_URL=
# TWENTY_API_TOKEN=
# TWENTY_TOKEN=
# TWENTY_URL=
# UPTIME_KUMA_URL=
# VAULT_URL=

# ───────────────────────────────────────────────────────────────────────────────
#  23. Graph Backend Connections
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_NAME=
# CHAT_SESSION_ID=
# DEFAULT_VALIDATION_MODE=
# GRAPH_DB_HOST=
# GRAPH_DB_NAME=
# GRAPH_DB_PASSWORD=
# GRAPH_DB_POOL_TIMEOUT=5
# GRAPH_DB_PORT=6379
# GRAPH_DB_USER=
# GRAPH_FUSEKI_DATASET=
# GRAPH_FUSEKI_PASSWORD=
# GRAPH_FUSEKI_URL=
# GRAPH_FUSEKI_USER=
# GRAPH_PGGRAPH_SCHEMA=public
# GRAPH_PG_AGE=
# IS_KG_SERVER=
# LADYBUG_DB_READ_ONLY=0
# LADYBUG_MAX_DB_SIZE=
# LADYBUG_TRANSIENT_CONNECTIONS=
# OWL_ALLOW_REMOTE_IMPORTS=false
# OWL_BACKEND=
# OWL_DB_PATH=
# STARDOG_DATABASE=
# STARDOG_ENDPOINT=
# STARDOG_PASSWORD=
# STARDOG_USER=

# ───────────────────────────────────────────────────────────────────────────────
#  24. Graph Engine Service (sharding / connection)
# ───────────────────────────────────────────────────────────────────────────────
# GRAPH_COMPUTE_BACKEND=rust
# GRAPH_SERVICE_AUTH_SECRET=
# GRAPH_SERVICE_CHECKPOINT_SECS=300
# GRAPH_SERVICE_ENDPOINTS=
# GRAPH_SERVICE_PERSIST_DIR=
# GRAPH_SERVICE_PERSIST_ON_SHUTDOWN=true
# GRAPH_SERVICE_SOCKET=/tmp/epistemic-graph.sock
# GRAPH_SERVICE_TCP_ADDR=

# ───────────────────────────────────────────────────────────────────────────────
#  25. Knowledge Graph Engine Tuning
# ───────────────────────────────────────────────────────────────────────────────
# KG_ACL_DEFAULT_ALLOW=false
# KG_ADAPTIVE_CONCURRENCY=true
# KG_AGENT_AUTO_APPLY=false
# KG_ALLOW_FULL_SCAN=false
# KG_ASR_MODEL=
# KG_AUTH_REQUIRED=
# KG_AUTH_TOKEN=
# KG_AUTO_INGEST_SKILLS=true
# KG_BRAIN_ENFORCE=
# KG_BREADTH_LIBRARY_ROOTS=
# KG_BREADTH_REPO_ROOTS=
# KG_CARD_MODEL=lite
# KG_CHUNKED_DRAIN=true
# KG_CONCEPT_CODE_LINK=1
# KG_CONNECTIONS=
# KG_DAEMON_LOG_LEVEL=INFO
# KG_DEFAULT_GRAPH=__commons__
# KG_DEV_MODE=false
# KG_DRAIN_MAX_PAGES=
# KG_DRAIN_PAGE_SIZE=100
# KG_DSPY_OPTIMIZATION=false
# KG_DSPY_OPTIMIZATION_INTERVAL=3600.0
# KG_EA_WRITEBACK=false
# KG_EMBED_TIMEOUT=30.0
# KG_ENABLE_HARD_NEGATIVE_MINING=false
# KG_ENGINE_DETACHED=
# KG_ENGINE_INSECURE=false
# KG_ENGINE_POOL_DROP_ON_EVICT=false
# KG_ENGINE_POOL_SIZE=8
# KG_ENGINE_TOOL_POOL_SIZE=16
# KG_ENRICH_CHUNK_THRESHOLD=12000
# KG_ENRICH_MAX_CHUNKS=64
# KG_ENVELOPE_SHACL_GATE=0
# KG_EVAL_CAPTURE=false
# KG_EXTRACT_MAX_RETRIES=4
# KG_FAILURE_EVOLUTION_WINDOW=86400.0
# KG_FAILURE_REGRESSION_DATASET=false
# KG_FUSEKI_ENDPOINT=
# KG_FUSEKI_PUBLISH=false
# KG_FUSEKI_PUBLISH_INTERVAL=3600.0
# KG_GRAPH_NAME=__commons__
# KG_INGEST_ENGINE_ENDPOINT=
# KG_INGEST_GRAPH_ROUTING=false
# KG_INGEST_INFLIGHT=
# KG_INGEST_PROFILE=
# KG_INGEST_SHARD_FANOUT=false
# KG_LLM_PRIORITY_RESERVE=
# KG_LLM_PRIORITY_RESERVE_FRACTION=0.34
# KG_MEDIA_TENANT_ISOLATED_BLOBS=false
# KG_MIN_RELEVANCE_THRESHOLD=
# KG_PARSE_BATCH=512
# KG_POLICY_VERSION=
# KG_POOL_MEMORY_GEN_CAP=
# KG_PROCESS_WRITEBACK=false
# KG_PROVIDER_ADAPTER_BACKEND=static
# KG_RERANK_BASE_URL=
# KG_RERANK_LOCAL_NEURAL=false
# KG_RERANK_MODEL=
# KG_RESEARCH_EXTERNAL=0
# KG_RESEARCH_FEED=true
# KG_RESEARCH_FEED_INTERVAL=1800.0
# KG_RETRIEVAL_QUALITY_GATE=true
# KG_RSS_FEEDS=
# KG_SCHED_CODEBASE_CAP=
# KG_SERVED_PROFILE=true
# KG_SERVER_HOST=127.0.0.1
# KG_SERVER_PORT=8100
# KG_STAGED_PIPELINE=1
# KG_STRICT_SOURCE_PARTITION=false
# KG_TASKS_PARTITIONS=6
# KG_TENANT_GC_INTERVAL=300.0
# KG_TRUST_HIERARCHY=
# KG_WATCH_DIRS=
# KG_WORKFLOW_SHAPE_GATE=true
# KG_WRITE_DELTA=1

# ───────────────────────────────────────────────────────────────────────────────
#  26. Knowledge Graph Core Tuning
# ───────────────────────────────────────────────────────────────────────────────
# ARD_REGISTRIES=
# CLAUDE_MEMORY_DIR=
# DOCKERHUB_NAMESPACE=
# DOCKERHUB_NAMESPACES=
# EPISTEMIC_GRAPH_AUTOSTART=
# EPISTEMIC_GRAPH_REDB_SHARDS=
# EPISTEMIC_GRAPH_SOCKET=/tmp/epistemic-graph.sock
# EVENT_BACKEND=
# FRESHRSS_MAX_BATCHES=3
# FRESHRSS_USE_NOVELTY=false
# GITLAB_TOKEN=
# GITLAB_URL=https://gitlab.com
# GRAPH_ROUTING_STRATEGY=hybrid
# REDPANDA_BROKERS=
# REDPANDA_CONSUMER_GROUP=
# REDPANDA_SECURITY_PROTOCOL=
# SOURCE_SYNC_ALLOW_EMPTY_TOMBSTONE=
# WORKING_SET_EVICTION_RATIO=0.25
# WORKING_SET_MAX_EDGES=200000
# WORKING_SET_MAX_NODES=50000
# WORKING_SET_TTL_SECONDS=3600

# ───────────────────────────────────────────────────────────────────────────────
#  27. Knowledge Graph (misc)
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_UTILITIES_KAFKA_LIVE=
# ASSIMILATION_ENGINE_PAGERANK=
# ASSIMILATION_SYNTH_TIMEOUT_S=30
# EPISTEMIC_GRAPH_SERVER_BIN=

# ───────────────────────────────────────────────────────────────────────────────
#  28. Autonomous Evolution Loop Toggles
# ───────────────────────────────────────────────────────────────────────────────
# KG_INSIGHT_AUTONOMY=false
# KG_LOOP_AUTO_DEVELOP=false
# KG_LOOP_BELIEF_REVISION=true
# KG_LOOP_BREADTH=true
# KG_LOOP_DISCOVER=false
# KG_LOOP_DISTILL=false
# KG_LOOP_INSIGHT_VALIDATION=true
# KG_LOOP_MINE_DISCOVERY=true
# KG_LOOP_STANDARDIZE=false
# KG_LOOP_TRACE_MINING=true
# KG_SAI_FACTORY=true
# KG_SAI_FACTORY_INTERVAL=3600.0

# ───────────────────────────────────────────────────────────────────────────────
#  29. Ingestion Tuning
# ───────────────────────────────────────────────────────────────────────────────
# AGENTS_INSECURE_SSL=0

# ───────────────────────────────────────────────────────────────────────────────
#  30. Enrichment Pipeline
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_USER_MAP={}

# ───────────────────────────────────────────────────────────────────────────────
#  31. Research & Assimilation
# ───────────────────────────────────────────────────────────────────────────────
# FRESHRSS_URL=
# PLACEMENT_CONTROL_LOOP_ENABLED=false

# ───────────────────────────────────────────────────────────────────────────────
#  32. Skill-Graph Distillation
# ───────────────────────────────────────────────────────────────────────────────
# SKILL_GRAPH_CRAWLER=
# SKILL_GRAPH_CRAWLER_PYTHON=
# SKILL_GRAPH_CRAWL_TIMEOUT=
# SKILL_GRAPH_MAX_PAGES=

# ───────────────────────────────────────────────────────────────────────────────
#  33. Ontology / Connector Governance
# ───────────────────────────────────────────────────────────────────────────────
# AGENTS_ROOT=
# CONNECTOR_MANIFEST_REQUIRE_ENTERPRISE=

# ───────────────────────────────────────────────────────────────────────────────
#  34. Database Environment Setup
# ───────────────────────────────────────────────────────────────────────────────
# FUSEKI_ENDPOINT=http://localhost:3030

# ───────────────────────────────────────────────────────────────────────────────
#  35. Multi-Instance Source Connectors
# ───────────────────────────────────────────────────────────────────────────────
# CONFLUENCE_INSTANCES=
# GITLAB_INSTANCES=
# JIRA_INSTANCES=
# PLANE_INSTANCES=

# ───────────────────────────────────────────────────────────────────────────────
#  36. Source Connectors
# ───────────────────────────────────────────────────────────────────────────────
# ARD_REQUIRE_SIGNATURE=false
# CONNECTOR_DEFAULT_PUBLIC=false
# JINA_API_KEY=

# ───────────────────────────────────────────────────────────────────────────────
#  37. External Service Endpoints
# ───────────────────────────────────────────────────────────────────────────────
# ARCHIVEBOX_URL=
# JENA_FUSEKI_URL=
# KAFKA_BOOTSTRAP_SERVERS=
# KAFKA_ENABLED=false
# KAFKA_ENABLE_WRITE=false
# KAFKA_TOPIC=
# NATS_URL=
# PGGRAPH_DSN=
# SPARQL_ENDPOINTS=["https://query.wikidata.org/sparql"]
# VLLM_BASE_URL=

# ───────────────────────────────────────────────────────────────────────────────
#  38. MCP Multiplexer & Child Process Management
# ───────────────────────────────────────────────────────────────────────────────
# MCP_CHILD_BREAKER_COOLDOWN=15.0
# MCP_CHILD_BREAKER_THRESHOLD=5
# MCP_CHILD_MAX_CONCURRENCY=8
# MCP_CHILD_MAX_RESTARTS=5
# MCP_CHILD_POOL_SIZE=1
# MCP_CHILD_QUEUE_TIMEOUT=30.0
# MCP_CHILD_RESTART_WINDOW=300.0
# MCP_DYNAMIC_ALWAYS_ON=lambda: ["graph-os"]
# MCP_DYNAMIC_TOP_K=8
# MCP_MULTIPLEXER_MODE=eager

# ───────────────────────────────────────────────────────────────────────────────
#  39. MCP Server / OIDC Auth
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_ID=
# GRAPH_FANOUT_TIMEOUT=
# KEYCLOAK_CLIENT_ID=
# KEYCLOAK_REALM=master
# KEYCLOAK_URL=
# OIDC_AUDIENCE=agent-services
# OIDC_BASE_URL=
# OIDC_ISSUER=
# OIDC_SCOPE=
# OIDC_TOKEN_URL=
# OPENAPI_CLIENT_ID=
# OPENAPI_CLIENT_SECRET=
# OPENAPI_PASSWORD=
# OPENAPI_USERNAME=
# SESSION_ID=

# ───────────────────────────────────────────────────────────────────────────────
#  40. Security & Secrets
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_SECRETS_MASTER_KEY=
# AGENT_UTILITIES_RUNTIME_DIR=
# ARD_SIGNING_PRIVATE_KEY=
# MAX_TOOL_CALLS_PER_SESSION=50
# MAX_TOOL_REPEATS=3
# OIDC_TLS_VERIFY=true
# SECURITY_PROMPT_THRESHOLD=0.8
# SOURCE_CREDENTIALS={}
# VAULT_ROLE_ID=
# VAULT_SECRET_ID=
# VAULT_TOKEN=

# ───────────────────────────────────────────────────────────────────────────────
#  41. Observability & Escalation
# ───────────────────────────────────────────────────────────────────────────────
# ESCALATION_BLAST_FANOUT=20
# ESCALATION_CI_RETRY_CAP=3
# ESCALATION_DIFF_FILES=10
# ESCALATION_REWARD_FLOOR=0.3
# HITL_ESCALATION_TIMEOUT=300
# INCIDENT_NOTIFY_URL=
# INCIDENT_TICKET_BACKEND=none
# INCIDENT_TICKET_ENABLE=false
# OTEL_SERVICE_NAME=
# TRM_WRITEBACK_BACKEND=none

# ───────────────────────────────────────────────────────────────────────────────
#  42. Orchestration & Dispatch
# ───────────────────────────────────────────────────────────────────────────────
# ACTION_IRREVERSIBILITY_AVERSION=false
# AGENT_CLAIM_BACKEND=
# DURABLE_EXECUTION_DB=
# FLEET_MCP_DOMAIN=arpa

# ───────────────────────────────────────────────────────────────────────────────
#  43. Engine Lifecycle & Circuit Breaker
# ───────────────────────────────────────────────────────────────────────────────
# CIRCUIT_BREAKER_THRESHOLD=3
# ENGINE_BREAKER_COOLDOWN=15.0
# ENGINE_BREAKER_THRESHOLD=5
# ENGINE_ENDPOINT=
# ENGINE_IDLE_SHUTDOWN_SECS=60
# ENGINE_LIFECYCLE=refcounted
# ENGINE_MODE=auto
# ENGINE_SURFACETOOL=true

# ───────────────────────────────────────────────────────────────────────────────
#  44. Agent Bus / Dispatch
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_BUS_LOG_BACKEND=
# AGENT_BUS_PARTITIONS=6
# AGENT_DISPATCH_BACKEND=inline
# AGENT_EXECUTION_TIMEOUT=120.0
# AGENT_TURNS_PARTITIONS=6
# AGENT_USER_TOKEN=

# ───────────────────────────────────────────────────────────────────────────────
#  45. Agent Runtime
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_THINKING_EFFORT=
# AGENT_UTILITIES_TESTING=

# ───────────────────────────────────────────────────────────────────────────────
#  46. State & Usage Storage
# ───────────────────────────────────────────────────────────────────────────────
# STATE_DB_POOL_SIZE=8
# STATE_DB_URI=
# TASK_QUEUE_BACKEND=
# USAGE_DB_BACKEND=sqlite
# USAGE_DB_PATH=
# USAGE_DB_URI=
# USAGE_DUCKDB_PATH=
# USAGE_GATEWAY_URL=
# USAGE_TENANT_ID=
# USAGE_TRACKING_ENABLED=true

# ───────────────────────────────────────────────────────────────────────────────
#  47. Gateway Rate Limiting
# ───────────────────────────────────────────────────────────────────────────────
# GATEWAY_METRICS=false
# GATEWAY_RATE_BURST=0.0
# GATEWAY_RATE_LIMIT=0.0
# GATEWAY_WORKERS=1

# ───────────────────────────────────────────────────────────────────────────────
#  48. Parallelism & Synthesis Tuning
# ───────────────────────────────────────────────────────────────────────────────
# ENABLE_PROGRESSIVE_SYNTHESIS=true
# MAX_PARALLEL_AGENTS=60
# PARALLEL_BATCH_SIZE=25
# PLACEMENT_CATALOG_ENABLED=true
# PLACEMENT_CATALOG_TTL_S=5.0
# SYNTHESIS_RATIO=10
# SYNTHESIS_STRATEGY=auto
# WORKER_POOL_SIZE=8

# ───────────────────────────────────────────────────────────────────────────────
#  49. Graph Routing & Planning
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_REQUEST_LIMIT=
# AGENT_UTILITIES_GWT_STRICT=
# ARPO_BRANCH_ENTROPY=0.6
# ARPO_MAX_BRANCHES=3
# GRAPH_DIRECT_DISPATCH=true
# PLANNER_REQUEST_LIMIT=6
# QUERY_ROUTER_L1_THRESHOLD=50000
# QUERY_ROUTER_STRATEGY=
# VERIFIER_REQUEST_LIMIT=4

# ───────────────────────────────────────────────────────────────────────────────
#  50. KV-Cache Layering
# ───────────────────────────────────────────────────────────────────────────────
# EPISTEMIC_GRAPH_KVCACHE_ADDR=
# EPISTEMIC_GRAPH_KVCACHE_MAX_CONNECTIONS=32
# EPISTEMIC_GRAPH_KVCACHE_TIMEOUT_S=2.0
# EPISTEMIC_GRAPH_KVCACHE_TLS_VERIFY=true
# EPISTEMIC_GRAPH_KVCACHE_TOKEN=
# EPISTEMIC_GRAPH_KVCACHE_URL=
# KV_CACHE_CHARS_PER_TOKEN=4.0
# KV_CACHE_LAYERING=true
# KV_CACHE_MIN_CONTEXT_TOKENS=2048
# KV_CACHE_MIN_HISTORY_TURNS=1
# KV_CACHE_MIN_PREFIX_TOKENS=1024

# ───────────────────────────────────────────────────────────────────────────────
#  51. RLM Sandbox
# ───────────────────────────────────────────────────────────────────────────────
# ENABLE_RLM=false
# FORKD_SNAPSHOT_TAG=pyagent
# FORKD_TOKEN=
# FORKD_URL=http://127.0.0.1:8889
# RLM_LIVE_BENCH=
# RLM_SANDBOX=auto
# RLM_USE_CONTAINER=false
# RLM_USE_MONTY=false
# RLM_USE_WASM=false
# RLM_WASM_PYTHON=

# ───────────────────────────────────────────────────────────────────────────────
#  52. Media Generation / Transcription Gateways
# ───────────────────────────────────────────────────────────────────────────────
# COMFYUI_URL=http://comfyui.arpa:8188
# FASTER_WHISPER_URL=http://faster-whisper.arpa:8000
# FLUX_URL=
# HUNYUAN_IMAGE_URL=
# HUNYUAN_URL=
# LTX_URL=
# OPENAI_TTS_URL=http://xtts.arpa:5002
# QWEN_IMAGE_URL=
# SD35_URL=
# XTTS_URL=http://xtts.arpa:5002

# ───────────────────────────────────────────────────────────────────────────────
#  53. Ecosystem Integrations
# ───────────────────────────────────────────────────────────────────────────────
# ARCHI_MODEL_PATH=
# ARD_FEDERATION_MODE=none
# ARD_PUBLISHER_DOMAIN=
# ARD_SPEC_VERSION=draft-0
# HERMES_HOME=
# LEANIX_API_TOKEN=
# LEANIX_TOKEN=
# LEANIX_URL=
# LEANIX_VERIFY_SSL=false

# ───────────────────────────────────────────────────────────────────────────────
#  54. Finance / Crypto Connectors
# ───────────────────────────────────────────────────────────────────────────────
# BINANCE_API_KEY=
# BINANCE_SECRET=
# BINANCE_SECRET_KEY=
# DERIVATIVES_API_KEY=
# ETHERSCAN_API_KEY=

# ───────────────────────────────────────────────────────────────────────────────
#  55. Schema Pack
# ───────────────────────────────────────────────────────────────────────────────
# ENABLE_KG_EXTERNAL_GRAPHS=true
# ENABLE_KG_KB=true
# ENABLE_KG_ONTOLOGY_BOOTSTRAP=false
# ENABLE_KG_OWL=true
# ENABLE_KG_SHACL_GATE=true
# ENABLE_KG_WORKSPACE_SYNC=true
# GRAPH_SCHEMA_AUDIT_DIR=
# GRAPH_SCHEMA_AUDIT_VERBOSE=
# GRAPH_SCHEMA_PACK=

# ───────────────────────────────────────────────────────────────────────────────
#  56. Core Runtime Tuning
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_UTILITIES_CACHE_DIR=
# AGENT_UTILITIES_DATA_DIR=
# AGENT_UTILITIES_LOG_DIR=
# AGENT_UTILITIES_MEMORY_DIR=
# AGENT_UTILITIES_PROMPTS_DIR=
# AGENT_UTILITIES_SKILLS_DIR=
# CONTINUOUS_STARDOG_MIRROR=false
# ENABLE_KG_REGISTRY_FETCH=true
# ENABLE_SDD_WATCHER=true
# EVOLUTION_WORKTREE_ROOT=
# GPU_CONCURRENCY_BUDGETS={}
# GPU_RESERVED_ROLES=
# MODEL_AUTOSCALE_UPDATE_INTERVAL_S=
# MODEL_AUTOSCALE_UPDATE_SAMPLES=
# MODEL_AUTOSCALE_VLLM_METRICS=true
# MODEL_AUTOSCALE_WINDOW=
# MODEL_BREAKER_BACKOFF_FACTOR=
# MODEL_BREAKER_BASE_COOLDOWN_S=
# MODEL_BREAKER_FAIL_THRESHOLD=
# MODEL_BREAKER_MAX_COOLDOWN_S=
# MODEL_CIRCUIT_BREAKER=true
# MODEL_LATENCY_GRADIENT_TARGET=
# MODEL_MAX_CONCURRENCY=
# MODEL_MAX_CONCURRENT_REQUESTS=
# MODEL_ROLE_ROUTING=
# POSTGRES_DSN=
# PRICING_LITELLM_URL=
# REDIS_URL=
# RESOURCE_WEIGHT_COST=0.4
# RESOURCE_WEIGHT_LATENCY=0.3
# RESOURCE_WEIGHT_QUALITY=0.3
# SESSION_COST_BUDGET_USD=5.0
# SESSION_LATENCY_BUDGET_MS=30000
# SESSION_TOKEN_BUDGET=500000
# TELEGRAM_BOT_TOKEN=

# ───────────────────────────────────────────────────────────────────────────────
#  57. SDD Watcher
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_INVENTORY_YAML=
# AGENT_MCP_CONFIG_JSON=
# AGENT_RESEARCH_DIR=
# AGENT_SKILLS_DIR=
# SCHOLARX_PAPERS_DIR=

# ───────────────────────────────────────────────────────────────────────────────
#  58. Harness / MemoryData
# ───────────────────────────────────────────────────────────────────────────────
# GRAPHOS_BASE_URL=http://127.0.0.1:8000
# GRAPHOS_TOKEN=

# ───────────────────────────────────────────────────────────────────────────────
#  59. Deployment / Doctor
# ───────────────────────────────────────────────────────────────────────────────
# APP_PROFILE=

# ───────────────────────────────────────────────────────────────────────────────
#  60. Testing / Live-Integration Opt-ins (contributors)
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_UTILITIES_TEST_REPO_GUARD=warn
# CAMUNDA7_URL=

# ───────────────────────────────────────────────────────────────────────────────
#  61. Maintainer / CI Scripts (repo-internal tooling)
# ───────────────────────────────────────────────────────────────────────────────
# AGENT_PACKAGES_ROOT=
# AGENT_URL=http://localhost:8000
# AGENT_UTILITIES_TOKEN_SECRET=
# EG_CAPABILITIES_LEDGER=

# ───────────────────────────────────────────────────────────────────────────────
#  62. Misc
# ───────────────────────────────────────────────────────────────────────────────
# EPISTEMIC_GRAPH_ENDPOINT=
# GRAPH_ENGINE_ENDPOINT=
# SSL_VERIFY=true
