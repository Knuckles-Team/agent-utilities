# Optional dependency override file for published-package builds.
#
# Intentionally empty. The first-party pydantic-ai-harness ACP adapter follows
# the current Pydantic AI line, so ACP no longer needs a resolver override.
