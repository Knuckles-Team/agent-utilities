# uv dependency override (uv pip install --override overrides.txt / UV_OVERRIDE).
#
# pydantic-acp 1.4.0 (the optional `[acp]` extra) declares
# `pydantic-ai-slim>=2.0.0,<=2.9.1` — no longer the old v1 exact pin
# (`==1.106.0`), but still short of the 2.14.1 floor the rest of this project
# runs on. This relaxes that transitive cap so any acp-inclusive extra ([acp],
# [test], [all]) resolves cleanly on the current v2 floor instead of being
# dragged back to 2.9.1.
#
# Mirrors `[tool.uv] override-dependencies` in pyproject.toml (which covers
# uv lock/sync; this file covers `uv pip install`). Remove both once pydantic-acp
# ships a release whose own cap admits 2.14.1+.
# See docs/architecture/pydantic-ai-v2-migration.md.
pydantic-ai-slim>=2.14.1,<3.0.0
