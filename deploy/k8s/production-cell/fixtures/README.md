# Production-cell negative fixtures

These files are deliberately incomplete inputs. They are static rejection
fixtures for the topology authority, not alternate production manifests and
not capacity claims. A renderer invocation must reject them before creating an
output directory. The focused root validation should also mutate the reviewed
`production-input.example.json` in memory to prove the deeper resource-pool
and retained-authority failures without committing measured or private data.
