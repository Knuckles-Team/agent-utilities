{
  "mcpServers": {
    "example-mcp": {
      "command": "example-mcp",
      "args": [
        "--transport",
        "stdio"
      ],
      "env": {
        "API_KEY": "secret://example-mcp/api-key"
      }
    }
  }
}
