{
  "mcpServers": {
    "example-mcp": {
      "command": "uv",
      "args": [
        "run",
        "example-mcp",
        "--transport",
        "stdio"
      ],
      "env": {
        "API_KEY": "secret://example-mcp/api-key"
      }
    }
  }
}
