{
  "mcpServers": {
    "example-mcp": {
      "command": "uv",
      "args": [
        "run",
        "example-mcp",
        "--transport",
        "stdio"
      ],
      "env": {
        "MCP_TOOL_MODE": "intent",
        "API_KEY": "secret://example-mcp/api-key"
      }
    }
  }
}
