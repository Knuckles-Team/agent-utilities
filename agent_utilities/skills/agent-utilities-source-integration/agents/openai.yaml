interface:
  display_name: "Agent Utilities Source Integration"
  short_description: "Connect and sync an external source into the KG"
  default_prompt: "Use $agent-utilities-source-integration to configure, discover, mirror, and sync the named external source into the Knowledge Graph."
