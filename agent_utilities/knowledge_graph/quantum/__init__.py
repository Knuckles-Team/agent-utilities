"""Q8 -- the agent-facing quantum control plane (graph_quantum MCP surface).

See ``budget.py`` (the quota/cost-budget enforcement gate) and
``agent_utilities.observability.quantum_trace`` (the ``:QuantumJob`` provenance
node wired into the existing ``:ToolCall``/``:RunTrace`` model).
"""
