[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-utilities"
version = "0.1.12"
description = "Agent Utilities for Pydantic AI Agents"
readme = "README.md"
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Environment :: Console",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.10"
dependencies = [
    "tree-sitter>=0.23.2",
    "requests>=2.32.5",
    "urllib3>=2.3.0",
    "pydantic[email]>=2.8.2",
    "PyYAML>=6.0.0",
]

[[project.authors]]
name = "Audel Rouhi"
email = "knucklessg1@gmail.com"

[project.license]
text = "MIT"

[project.optional-dependencies]
mcp = [
    "fastmcp>=3.0.2",
    "eunomia-mcp>=0.3.10",
    "fastapi>=0.131.0",
    "httpx>=0.28.1",
]
agent = [
    "agent-utilities[mcp]>=0.1.12",
    "universal-skills>=0.1.8",
    "pydantic-ai-skills>=0.4.1",
    "pydantic-ai-slim[fastmcp,openai,anthropic,groq,mistral,google,huggingface,a2a,ag-ui,web]>=1.60.0",
]
embeddings = [
    "llama-index-core>=0.14.15",
    "llama-index-embeddings-openai>=0.5.1",
    "llama-index-embeddings-huggingface>=0.6.1",
    "llama-index-embeddings-ollama>=0.8.6",
]
all = [
    "agent-utilities[mcp,agent,embeddings]>=0.1.12",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
agent_utilities = ["agent/**", "skills/**"]

[tool.setuptools.packages.find]
where = ["."]
